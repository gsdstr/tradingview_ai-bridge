{
  "name": "tradingview-ai-bridge",
  "author": "gsdstr",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "turbo run format --continue -- --write --cache --cache-location .cache/.prettiercache",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "typecheck": "turbo run typecheck --filter=\"!./tooling/*\"",
    "test": "vitest run",
    "cli": "bun --filter cli dev",
    "mcp": "bun --filter mcp dev",
    "shared": "bun --filter @repo/shared test"
  },
  "devDependencies": {
    "@repo/prettier": "workspace:*",
    "prettier": "^3.9.6",
    "turbo": "^2.10.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=22",
    "bun": ">=1.3.14"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "apps/skills/*",
      "packages/*",
      "tooling/*"
    ],
    "catalog": {
      "@eslint/js": "^10.0.1",
      "@types/node": "^26.4.0",
      "prettier": "^3.9.6",
      "typescript": "^7.0.2"
    }
  }
}
