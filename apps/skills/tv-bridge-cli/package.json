{
  "name": "@repo/skill-tv-bridge-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Skill for TradingView AI Bridge CLI",
  "scripts": {
    "build": "pnpm --filter cli bundle && mkdir -p scripts && cp ../../cli/dist/tv-bridge-cli.js ./scripts/"
  }
}
