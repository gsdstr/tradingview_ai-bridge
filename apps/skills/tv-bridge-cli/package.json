{
  "name": "@repo/skill-tv-bridge-cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Agent Skill for TradingView AI Bridge CLI",
  "scripts": {
    "build": "bun --filter cli bundle && mkdir -p scripts && cp ../../cli/dist/tv-bridge-cli.mjs ./scripts/ && node ../validate-skills.mjs",
    "validate": "node ../validate-skills.mjs"
  }
}
